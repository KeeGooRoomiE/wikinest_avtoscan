# Compact — 2026-06-19

## Что делали

1. **Импорт регламентов из PDF/DOCX** в `docs/tp/reglamentation/add-car-reglament.md`:
   - AXENTA (из DOCX) — с изображениями в `docs/assets/images/axenta/`
   - Глонасс Soft (из PDF) — с изображениями в `docs/assets/images/glonassoft/`
   - Wialon (из PDF) — с изображениями в `docs/assets/images/wialon/`
   - Финальный шаг через account.avtoscan42.ru вынесен в отдельный раздел (убрали дублирование)

2. **Новый документ** `docs/tp/reglamentation/add-object-axenta.md` (из PDF):
   - Два способа создания объекта, поля формы, доп. настройки
   - Датчики (зажигание, напряжение, CAN, доп. оборудование, топливо)
   - Таблицы фильтрации топлива по типам ТС
   - Детектор поездок

3. **Главная страница** `docs/home.md` переписана:
   - Таблицы: регламенты, сервисы мониторинга (с URL), терминалы, быстрые ссылки

4. **UI sidebar**: кнопка `+` рядом с rename, появляется при hover на папку  
   Модальное окно: выбор папки (select) + имя страницы (input) + live preview пути

5. **Фикс изображений на GitHub Pages**: заменили все относительные пути на  
   `https://raw.githubusercontent.com/KeeGooRoomiE/wikinest_avtoscan/HEAD/docs/assets/images/...`

6. **SSH ключ** для пуша: `~/.ssh/id_ed25519_wikinest`, host `keegooroomie-github.com`

## Решения

- Изображения в WikiNest SPA всегда должны быть с полным raw.githubusercontent.com URL
- Пуш-паттерн: `git stash && git pull --rebase && git stash pop && git push` (settings.local.json нестейджед)
- Inline `<style>` в `<head>` index.html как fallback против кэша браузера для новых CSS правил

## Незавершённое

- Ничего критического. Все запрошенные задачи выполнены и запушены.
- `tp/reglamentation/index.md` существует но пустой
- Wialon: регламент создания объекта в Wialon отсутствует (есть только создание УЗ)
