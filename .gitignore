.claude/settings.local.json
CREDENTIALS.md
CREDENTIALS_actual.md
.env
VM_SETUP.md
VM_SETUP_root_ca.crt
graphify-out/

#Ansible
# SSH deploy keypair — never commit, even the .pub (it pins the private key's presence here)
ansible/inventory.ini
ansible/group_vars/all.yml

ansible/wikinest-vm
ansible/wikinest-vm.pub

# Video attachments — too large for git history, hosted on the server
# outside git instead (see backend/app.py upload_file, which skips the
# commit step for these extensions since git add would fail anyway).
*.mp4
*.mov
*.webm
*.avi
*.mkv
*.m4v
*.3gp
